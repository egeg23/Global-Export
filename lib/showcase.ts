/**
 * Демонстрационная площадка отличается от боевого сайта одной переменной.
 *
 * На ней корень открывает витрину, а панель управления открывается вообще без
 * входа. На настоящем сайте компании ни того, ни другого быть не должно,
 * поэтому проверка вынесена в одно место, а не размазана по страницам.
 */
export const isShowcase = process.env.SHOWCASE_ROOT === "true";

/** Домен, который подставляется, если в поле входа ввели имя без почты. */
export const loginDomain = "globalex.uz";

/**
 * Учётная запись, под которой площадка сама открывает панель.
 *
 * Пароль не спрашивается у посетителя, но нужен серверу: писать в базу можно
 * только от опознанного пользователя из списка администраторов — это правило
 * стоит в самой базе, и обходить его ключом сервисной роли ради демонстрации
 * значило бы отдать открытой странице полный доступ к данным.
 *
 * Значения по умолчанию совпадают с учёткой, заведённой при развёртывании
 * площадки. Секрета здесь нет: раньше эта же пара была написана прямо на
 * странице входа.
 */
export const showcaseAccount = {
  email: process.env.SHOWCASE_DEMO_EMAIL || `demo@${loginDomain}`,
  password: process.env.SHOWCASE_DEMO_PASSWORD || "demo",
};
