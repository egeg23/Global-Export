/**
 * Демонстрационная площадка отличается от боевого сайта одной переменной.
 *
 * На ней корень открывает витрину, а на странице входа показывается общий
 * демо-доступ. На настоящем сайте компании ни того, ни другого быть не должно,
 * поэтому проверка вынесена в одно место, а не размазана по страницам.
 */
export const isShowcase = process.env.SHOWCASE_ROOT === "true";

/** Домен, который подставляется, если в поле входа ввели имя без почты. */
export const loginDomain = "globalex.uz";
