/**
 * Типы витрины FOODMAXX.
 *
 * Третий проект в этом репозитории: сайт для ООО «Global Food Exclusive»,
 * которое выпускает консервацию под этим брендом. Живёт отдельно от соседей —
 * свой каталог, свои токены, свой корень маршрутов `/foodmaxx`.
 */

/** Три полки, на которые компания сама делит ассортимент. */
export type Shelf = "ovoshi" | "myaso" | "blyuda";

export type Product = {
  slug: string;
  name: string;
  shelf: Shelf;
  /** Файл в `/public/foodmaxx/sets`, без расширения. */
  image: string;
  /**
   * Приписка с этикетки — «тушёная кусковая», «элитная», «халяль».
   * Ничего не додумано: что напечатано на банке, то и здесь.
   */
  note?: string;
  /** Тара, если её видно на снимке. */
  pack?: "steklo" | "zhest" | "butylka";
};
