/**
 * Глубина первого экрана Golden House.
 *
 * Сами хуки переехали в `components/showcase/depth.ts`: они понадобились
 * второй витрине, и держать общий механизм в папке одного заказчика стало
 * неправильно. Здесь остался реэкспорт, чтобы не трогать работающие
 * компоненты GH.
 */

export { useDepth, useStage } from "@/components/showcase/depth";
